# ============================================================
# SystemX Backend Environment Configuration Template
# ============================================================
# 说明: 环境变量模板文件
# 使用方法:
#   1. 复制此文件: cp .env.example .env
#   2. 编辑 .env 填入真实的配置值
#   3. .env 文件不会被提交到 Git（已在 .gitignore 中）
# ============================================================

# ========== 数据库配置 ==========
# 开发环境推荐使用 SQLite（无需额外安装数据库服务器）
DATABASE_URL=sqlite://./systemx.db

# 生产环境可使用 PostgreSQL：
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# ========== 应用环境标识 ==========
# 可选值: dev (开发) / staging (测试) / production (生产)
ENV=dev

# ========== JWT 认证配置 ==========
# ⚠️ 重要: 必须生成强随机密钥！使用以下命令生成：
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-jwt-secret-here-CHANGE-THIS
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ========== OpenAI Whisper API 配置（语音识别 ASR）==========
WHISPER_API_URL=https://api.openai.com/v1/audio/transcriptions
WHISPER_MODEL=whisper-1
# 在 https://platform.openai.com/api-keys 获取 API 密钥
# 暂时不用可留空
OPENAI_API_KEY=

# ========== ElevenLabs TTS API 配置（文字转语音）==========
ELEVENLABS_API_URL=https://api.elevenlabs.io/v1
# 在 https://elevenlabs.io/ → Profile → API Keys 获取
# 暂时不用可留空
ELEVENLABS_API_KEY=
DEFAULT_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# ========== 多口音语音 ID 配置 ==========
# 说明：以下为不同英语口音的语音 ID
# 当前使用相同的默认 ID，可在 ElevenLabs Voice Library 中选择不同口音替换
VOICE_ID_AMERICAN=21m00Tcm4TlvDq8ikWAM
VOICE_ID_AUSTRALIA=21m00Tcm4TlvDq8ikWAM
VOICE_ID_BRITISH=21m00Tcm4TlvDq8ikWAM
VOICE_ID_CHINESE=21m00Tcm4TlvDq8ikWAM
VOICE_ID_INDIA=21m00Tcm4TlvDq8ikWAM

# 不同口音示例（可选）：
# VOICE_ID_AMERICAN=EXAVITQu4vr4xnSDxMaL
# VOICE_ID_AUSTRALIA=IKne3meq5aSn9XLyUdCD
# VOICE_ID_BRITISH=JBFqnCBsd6RMkjVDRZzb
# VOICE_ID_CHINESE=hkfHEbBvdQFNX4uWHqRF
# VOICE_ID_INDIA=kL06KYMvPY56NluIQ72m

# ========== 服务器配置 ==========
HOST=0.0.0.0
PORT=8000

# ============================================================
# 快速开始:
# 1. 复制此文件: cp .env.example .env
# 2. 生成 JWT 密钥: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. 将生成的密钥填入 JWT_SECRET
# 4. 其他配置保持默认即可开始开发
# 5. 需要 ASR/TTS 功能时再填入对应 API 密钥
# ============================================================
