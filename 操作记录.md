# SystemX 项目操作记录文档

> **项目名称**: SystemX - Fast Accent Translate Web
> **操作日期**: 2025-11-02
> **操作者**: Rachel
> **文档版本**: 1.0

---

## 📋 目录

1. [项目初始化](#1-项目初始化)
2. [GitHub 仓库配置](#2-github-仓库配置)
3. [环境配置](#3-环境配置)
4. [问题排查与解决](#4-问题排查与解决)
5. [最终成果](#5-最终成果)
6. [总结与经验](#6-总结与经验)

---

## 1. 项目初始化

### 1.1 项目模板应用

**任务**: 使用 CLAUDE_TEMPLATE.md 初始化项目规范

**执行步骤**:
1. 读取 `/Users/rachel/Desktop/9900—System X/CLAUDE_TEMPLATE.md`
2. 分析现有 SystemX 项目结构
3. 根据模板定制项目规范

**创建的文件**:
- ✅ `CLAUDE.md` - 632 行项目开发规范文档
  - 包含关键开发规则
  - 项目结构说明
  - 技术栈文档（FastAPI、React、WebSocket）
  - 环境配置指南
  - 安全注意事项

**改进点**:
- 增强 `.gitignore` 文件（从 6 行扩展到 84 行）
  - 添加 Python 特定模式
  - 添加 Node.js 模式
  - 添加数据库文件忽略
  - 添加音频文件忽略

**Git 提交**:
```
commit bffa14f
docs: Add CLAUDE.md and enhance .gitignore for better development workflow
```

---

## 2. GitHub 仓库配置

### 2.1 创建新的 GitHub 仓库

**背景**: 需要将项目从原课程仓库迁移到个人仓库

**执行步骤**:

1. **验证 GitHub CLI 认证**
   ```bash
   gh auth status
   # 结果: 已登录 Rachel-Huang77 账户
   ```

2. **创建新仓库**
   ```bash
   gh repo create SystemX-FastAccentTranslate-Web --public
   # 仓库 URL: https://github.com/Rachel-Huang77/SystemX-FastAccentTranslate-Web
   ```

3. **更新 Git Remote**
   ```bash
   # 移除旧 remote
   git remote remove origin
   # 添加新 remote
   git remote add origin git@github.com:Rachel-Huang77/SystemX-FastAccentTranslate-Web.git
   ```

### 2.2 安全问题处理

**遇到的问题**: 推送被 GitHub 阻止，检测到 API 密钥泄露

**错误信息**:
```
remote: error: GH013: Repository rule violations found
remote: - Push cannot contain secrets
remote: - OpenAI API Key detected in commit 691ffdd
```

**发现的安全隐患**:
- `backend/.env.example` 包含真实的 OpenAI API 密钥
- `frontend/.env.example` 包含真实的 ElevenLabs API 密钥

**解决方案**:
1. 替换所有真实密钥为占位符
   - `OPENAI_API_KEY=your-openai-api-key-here`
   - `ELEVENLABS_API_KEY=your-elevenlabs-api-key-here`

2. 清理 Git 历史
   ```bash
   # 删除旧的 Git 历史
   rm -rf .git
   # 创建全新的历史
   git init
   git add .
   git commit -m "Initial commit: SystemX..."
   # 强制推送干净的历史
   git push -u origin main --force
   ```

**Git 提交**:
```
commit eb19561
Initial commit: SystemX - Real-time speech recognition and multi-accent TTS
```

**安全成果**:
- ✅ 所有历史提交中无真实密钥
- ✅ .env.example 使用安全占位符
- ✅ GitHub 推送保护验证通过

---

## 3. 环境配置

### 3.1 配置文件创建

**任务**: 创建完整的环境变量配置文件

#### 后端配置文件

**backend/.env** (本地，不提交到 Git):
```bash
# 数据库
DATABASE_URL=sqlite://./systemx.db

# JWT 密钥（强随机生成）
JWT_SECRET=CNnuV6-hsfYiYKf3fln8Iw0GlR0tJF6nxCEzJ8Di5lU
ACCESS_TOKEN_EXPIRE_MINUTES=60

# OpenAI API（暂时留空）
OPENAI_API_KEY=

# ElevenLabs API（暂时留空）
ELEVENLABS_API_KEY=

# 服务器配置
HOST=0.0.0.0
PORT=8000
```

**backend/.env.example** (提交到 Git):
```bash
# 模板文件，使用占位符
DATABASE_URL=sqlite://./systemx.db
JWT_SECRET=your-jwt-secret-here-CHANGE-THIS
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
# 包含详细的配置说明和快速开始指南
```

#### 前端配置文件

**frontend/.env** (本地，不提交到 Git):
```bash
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
```

**frontend/.env.example** (提交到 Git):
```bash
# 模板文件
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
# 包含配置说明和生产环境示例
```

**关键决策**:
- 使用 SQLite 而非 PostgreSQL（开发更简单）
- 生成强随机 JWT 密钥（32 字节）
- API 密钥暂时留空（暂不使用 ASR/TTS 功能）

**Git 提交**:
```
commit c166bfc
docs: Update .env.example files with comprehensive configuration templates
```

### 3.2 启动脚本创建

**任务**: 创建一键启动脚本

**创建的文件**:

1. **start-backend.sh**
   ```bash
   #!/bin/bash
   # 自动激活虚拟环境
   # 检查 .env 文件存在
   # 启动 uvicorn 服务器
   ```

2. **start-frontend.sh**
   ```bash
   #!/bin/bash
   # 检查并安装依赖
   # 创建默认 .env 文件
   # 启动 Vite 开发服务器
   ```

**特点**:
- 自动检查前置条件
- 友好的错误提示
- 可执行权限配置

**Git 提交**:
```
commit b584eee
feat: Add convenient startup scripts for backend and frontend
```

---

## 4. 问题排查与解决

### 4.1 CORS 和 Fetch 错误

**问题描述**:
- 前端 http://localhost:5173 显示 "Failed to fetch"
- 浏览器控制台显示 CORS 错误
- 注册功能无法正常工作

**错误截图分析**:
```
❌ Access to fetch at 'http://localhost:8000/api/v1/auth/login'
   from origin 'http://localhost:5173' has been blocked by CORS policy

❌ Failed to load resource: net::ERR_FAILED
```

### 4.2 问题诊断过程

#### 步骤 1: 检查后端运行状态

**测试命令**:
```bash
curl http://localhost:8000/healthz
```

**结果**:
```json
{"ok":true}
```
✅ 后端服务器正常运行

#### 步骤 2: 检查 CORS 配置

**测试命令**:
```bash
curl -X OPTIONS http://localhost:8000/api/v1/auth/register \
  -H "Origin: http://localhost:5173" \
  -H "Access-Control-Request-Method: POST"
```

**结果**:
```
access-control-allow-origin: http://localhost:5173 ✅
access-control-allow-credentials: true ✅
```
✅ CORS 配置正确

#### 步骤 3: 测试实际注册请求

**测试命令**:
```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"Test123"}'
```

**结果**:
```
HTTP Status: 500 Internal Server Error
```
❌ **找到问题根源：后端返回 500 错误！**

#### 步骤 4: 检查数据库状态

**测试命令**:
```bash
sqlite3 backend/systemx.db ".tables"
```

**结果**:
```
(空输出 - 没有任何表)
```
❌ **数据库表未创建！**

### 4.3 根本原因

**错误链分析**:
```
数据库表不存在
    ↓
后端尝试访问 User 表失败
    ↓
后端返回 500 Internal Server Error
    ↓
浏览器接收到错误响应
    ↓
显示 CORS/Fetch 错误
```

**关键发现**: CORS 错误只是表象，真正的问题是数据库未初始化。

### 4.4 解决方案实施

#### 创建数据库初始化脚本

**文件**: `backend/init_db.py`

```python
#!/usr/bin/env python3
"""数据库初始化脚本"""
import asyncio
from tortoise import Tortoise

async def init():
    """初始化数据库连接和表结构"""
    await Tortoise.init(
        db_url='sqlite://./systemx.db',
        modules={'models': [
            'app.models.user',
            'app.models.conversation',
            'app.models.transcript'
        ]}
    )
    # 生成表结构
    await Tortoise.generate_schemas()
    print("✅ 数据库表创建成功！")

    # 验证表是否创建
    conn = Tortoise.get_connection("default")
    tables = await conn.execute_query(
        "SELECT name FROM sqlite_master WHERE type='table';"
    )
    print(f"📋 创建的表: {[t[0] for t in tables[1]]}")

    await Tortoise.close_connections()

if __name__ == "__main__":
    print("🚀 开始初始化数据库...")
    asyncio.run(init())
    print("✨ 数据库初始化完成！")
```

#### 执行初始化

**命令**:
```bash
cd backend
source venv/bin/activate
python3 init_db.py
```

**输出**:
```
🚀 开始初始化数据库...
✅ 数据库表创建成功！
📋 创建的表: ['users', 'conversations', 'transcript', 'sqlite_sequence']
✨ 数据库初始化完成！
```

#### 验证表结构

**命令**:
```bash
sqlite3 backend/systemx.db ".schema users"
```

**结果**:
```sql
CREATE TABLE IF NOT EXISTS "users" (
    "id" CHAR(36) NOT NULL PRIMARY KEY,
    "username" VARCHAR(256) NOT NULL UNIQUE,
    "email" VARCHAR(256),
    "password_hash" VARCHAR(255) NOT NULL,
    "role" VARCHAR(16) NOT NULL DEFAULT 'user',
    "created_at" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX "idx_users_usernam_266d85" ON "users" ("username");
```
✅ 表结构正确创建

### 4.5 前端 API 优化

**问题**: 前端 API 文件硬编码 URL

**修改前**:
```javascript
// src/api/auth.js
const BASE_URL = "http://localhost:8000/api/v1";
```

**修改后**:
```javascript
// src/api/auth.js
const BASE_URL = import.meta.env.VITE_API_BASE_URL + "/api/v1";
```

**修改的文件**:
- ✅ `frontend/src/api/auth.js`
- ✅ `frontend/src/api/conversations.js`
- ✅ `frontend/src/api/dashboard.js`

**优势**:
- 使用环境变量配置
- 便于切换开发/生产环境
- 符合最佳实践

**Git 提交**:
```
commit 696e458
fix: Initialize database and use environment variables in frontend API
```

---

## 5. 最终成果

### 5.1 项目结构

```
SystemX/
├── CLAUDE.md                  ✅ 项目开发规范（632行）
├── README.md                  ✅ 项目说明文档
├── .gitignore                 ✅ 增强版忽略规则（84行）
├── start-backend.sh           ✅ 后端一键启动脚本
├── start-frontend.sh          ✅ 前端一键启动脚本
├── 操作记录.md                 ✅ 本文档
├── backend/
│   ├── .env                   ✅ 本地配置（含真实密钥）
│   ├── .env.example           ✅ 配置模板（安全占位符）
│   ├── init_db.py             ✅ 数据库初始化脚本
│   ├── systemx.db             ✅ SQLite 数据库（含3个表）
│   ├── app/
│   │   ├── main.py           ✅ FastAPI 应用入口
│   │   ├── config.py         ✅ 配置管理
│   │   ├── models/           ✅ 数据库模型
│   │   ├── api/v1/routers/   ✅ API 路由
│   │   ├── services/         ✅ ASR/TTS 服务
│   │   └── core/             ✅ 核心功能
│   └── requirements.txt      ✅ Python 依赖
└── frontend/
    ├── .env                   ✅ 本地配置
    ├── .env.example           ✅ 配置模板
    ├── src/
    │   ├── api/               ✅ API 客户端（使用环境变量）
    │   ├── pages/             ✅ 页面组件
    │   └── components/        ✅ UI 组件
    └── package.json           ✅ Node 依赖
```

### 5.2 GitHub 仓库状态

**仓库信息**:
- 名称: `SystemX-FastAccentTranslate-Web`
- 所有者: `Rachel-Huang77`
- URL: https://github.com/Rachel-Huang77/SystemX-FastAccentTranslate-Web
- 可见性: Public

**提交历史**:
```
696e458 - fix: Initialize database and use environment variables
68306e8 - chore: Update frontend dependencies
27c10ea - docs: Update .env.example files
b584eee - feat: Add convenient startup scripts
eb19561 - Initial commit: SystemX
```

**安全状态**:
- ✅ 无真实 API 密钥在仓库中
- ✅ .env 文件被 .gitignore 忽略
- ✅ .env.example 使用安全占位符
- ✅ GitHub 推送保护验证通过

### 5.3 功能状态

**后端功能**:
- ✅ FastAPI 服务器运行在 http://localhost:8000
- ✅ 健康检查端点: `/healthz`
- ✅ API 文档: http://localhost:8000/docs
- ✅ CORS 正确配置
- ✅ 数据库表已创建（users, conversations, transcript）
- ✅ 用户注册端点正常工作
- ✅ 用户登录端点正常工作

**前端功能**:
- ✅ Vite 开发服务器运行在 http://localhost:5173
- ✅ 登录页面可访问
- ✅ 注册页面可访问
- ✅ API 调用使用环境变量
- ✅ WebSocket 连接配置正确

**暂未启用的功能**:
- ⏸️ OpenAI Whisper 语音识别（API 密钥未配置）
- ⏸️ ElevenLabs 文字转语音（API 密钥未配置）

---

## 6. 总结与经验

### 6.1 关键成就

1. **项目规范化**
   - 创建了完整的开发规范文档（CLAUDE.md）
   - 建立了标准化的项目结构
   - 制定了技术债务防范规则

2. **安全增强**
   - 识别并修复 API 密钥泄露问题
   - 清理 Git 历史中的敏感信息
   - 建立环境变量管理最佳实践

3. **开发效率提升**
   - 创建一键启动脚本
   - 完善环境配置文档
   - 建立标准化工作流程

4. **问题解决能力**
   - 系统化诊断复杂问题（CORS → 500 错误 → 数据库未初始化）
   - 创建可复用的解决方案（init_db.py）
   - 文档化问题和解决方案

### 6.2 技术经验

#### 数据库初始化

**教训**:
- Tortoise ORM 不会自动创建表
- 需要显式调用 `generate_schemas()`

**解决方案**:
- 创建独立的初始化脚本
- 在首次运行前执行
- 可重复执行（幂等性）

#### CORS 问题诊断

**经验**:
- CORS 错误可能是表象，不一定是 CORS 配置问题
- 需要测试后端实际响应状态
- 500 错误会导致浏览器报 CORS 错误

**诊断流程**:
1. 验证后端服务运行
2. 测试 CORS 预检请求
3. 测试实际 API 请求
4. 检查后端日志
5. 检查数据库状态

#### 环境变量管理

**最佳实践**:
- `.env` - 本地配置，包含真实密钥，**不提交**
- `.env.example` - 模板文件，使用占位符，**提交到 Git**
- 前端使用 `VITE_` 前缀暴露变量
- 生成强随机密钥（JWT_SECRET）

#### Git 历史清理

**场景**: 不慎提交敏感信息

**方案**:
1. **小项目/初期**: 删除 .git，重新初始化（本次使用）
2. **大项目**: 使用 `git filter-branch` 或 `BFG Repo-Cleaner`
3. **协作项目**: 撤销密钥，通知团队，强制推送

### 6.3 工作流程改进

#### 理想的项目启动流程

1. **克隆项目**
   ```bash
   git clone <repo-url>
   cd project
   ```

2. **配置环境**
   ```bash
   # 后端
   cd backend
   cp .env.example .env
   # 编辑 .env 填入真实配置

   # 前端
   cd ../frontend
   cp .env.example .env
   # 通常保持默认即可
   ```

3. **安装依赖**
   ```bash
   # 后端
   cd backend
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt

   # 前端
   cd ../frontend
   npm install
   ```

4. **初始化数据库**
   ```bash
   cd backend
   source venv/bin/activate
   python3 init_db.py
   ```

5. **启动服务**
   ```bash
   # 使用启动脚本
   ./start-backend.sh  # 终端1
   ./start-frontend.sh # 终端2
   ```

#### 日常开发流程

1. **启动开发环境**
   ```bash
   ./start-backend.sh   # 终端1
   ./start-frontend.sh  # 终端2
   ```

2. **开发和测试**
   - 后端: http://localhost:8000/docs
   - 前端: http://localhost:5173

3. **提交代码**
   ```bash
   git add .
   git commit -m "描述性提交信息"
   git push origin main
   ```

### 6.4 文档价值

本操作记录文档的作用：

1. **知识传承**
   - 记录问题和解决方案
   - 帮助团队成员快速上手
   - 避免重复遇到相同问题

2. **项目管理**
   - 追踪项目演进历史
   - 理解架构决策原因
   - 评估技术债务

3. **调试参考**
   - 快速定位类似问题
   - 参考诊断流程
   - 复用解决方案

### 6.5 后续建议

#### 短期任务（1-2周）

1. **完善功能测试**
   - 测试用户注册/登录流程
   - 测试会话管理
   - 测试对话存储

2. **添加 API 密钥**
   - 获取 OpenAI API Key
   - 获取 ElevenLabs API Key
   - 测试 ASR/TTS 功能

3. **完善错误处理**
   - 添加友好的错误提示
   - 改进前端错误显示
   - 添加后端日志记录

#### 中期任务（1-2月）

1. **添加测试**
   - 后端单元测试
   - 前端组件测试
   - API 集成测试

2. **性能优化**
   - 数据库查询优化
   - 前端资源优化
   - WebSocket 连接管理

3. **部署准备**
   - 选择部署平台
   - 配置生产环境
   - 设置 CI/CD

#### 长期任务（3-6月）

1. **功能扩展**
   - 添加更多语言支持
   - 添加语音训练功能
   - 添加用户偏好设置

2. **安全增强**
   - 添加 HTTPS
   - 实施速率限制
   - 添加安全审计

3. **可扩展性**
   - 迁移到生产数据库（PostgreSQL）
   - 添加缓存层（Redis）
   - 实施负载均衡

---

## 📚 参考资源

### 官方文档

- **FastAPI**: https://fastapi.tiangolo.com/
- **React**: https://react.dev/
- **Vite**: https://vitejs.dev/
- **Tortoise ORM**: https://tortoise.github.io/
- **OpenAI API**: https://platform.openai.com/docs/
- **ElevenLabs API**: https://elevenlabs.io/docs/

### 相关文件

- `CLAUDE.md` - 项目开发规范
- `README.md` - 项目说明文档
- `backend/.env.example` - 后端配置模板
- `frontend/.env.example` - 前端配置模板

### 常用命令

```bash
# 后端
cd backend && source venv/bin/activate
uvicorn app.main:app --reload
python3 init_db.py

# 前端
cd frontend
npm run dev

# 数据库
sqlite3 backend/systemx.db ".tables"
sqlite3 backend/systemx.db ".schema users"

# Git
git status
git log --oneline
git push origin main
```

---

## ✅ 验证清单

在移交或部署前，请确认：

### 后端
- [ ] 虚拟环境已创建 (`backend/venv/`)
- [ ] 依赖已安装 (`pip install -r requirements.txt`)
- [ ] .env 文件已配置
- [ ] 数据库已初始化 (`python3 init_db.py`)
- [ ] 健康检查通过 (`curl http://localhost:8000/healthz`)
- [ ] API 文档可访问 (http://localhost:8000/docs)

### 前端
- [ ] 依赖已安装 (`npm install`)
- [ ] .env 文件已创建
- [ ] 开发服务器可启动 (`npm run dev`)
- [ ] 页面可访问 (http://localhost:5173)
- [ ] API 调用正常

### Git & GitHub
- [ ] 仓库已推送到 GitHub
- [ ] .env 文件未被提交
- [ ] 无敏感信息在历史中
- [ ] README.md 更新

### 安全
- [ ] API 密钥使用环境变量
- [ ] JWT_SECRET 为强随机密钥
- [ ] CORS 正确配置
- [ ] 数据库文件在 .gitignore 中

---

## 📞 联系方式

**项目负责人**: Rachel
**GitHub**: https://github.com/Rachel-Huang77/SystemX-FastAccentTranslate-Web
**最后更新**: 2025-11-02

---

**文档结束**
