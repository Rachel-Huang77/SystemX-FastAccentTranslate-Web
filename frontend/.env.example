# ============================================================
# SystemX Frontend Environment Configuration Template
# ============================================================
# 说明: 前端环境变量模板文件
# 使用方法:
#   1. 复制此文件: cp .env.example .env
#   2. 根据需要修改配置（通常保持默认即可）
#   3. .env 文件不会被提交到 Git（已在 .gitignore 中）
# 注意: Vite 项目中只有 VITE_ 开头的变量会暴露给客户端代码
# ============================================================

# ========== 后端 API 配置 ==========
# 后端 REST API 基础 URL（用于 HTTP 请求）
# 开发环境: http://localhost:8000
# 生产环境: https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:8000

# 后端 WebSocket 基础 URL（用于实时通信）
# 开发环境: ws://localhost:8000
# 生产环境: wss://api.yourdomain.com (注意 HTTPS 对应 wss://)
VITE_WS_BASE_URL=ws://localhost:8000

# ============================================================
# 配置说明:
#
# 1. VITE_API_BASE_URL:
#    - 指向后端服务器的 HTTP 地址
#    - 不要在末尾添加斜杠 /
#
# 2. VITE_WS_BASE_URL:
#    - 指向后端服务器的 WebSocket 地址
#    - HTTP 使用 ws://，HTTPS 使用 wss://
#
# 3. 修改后需要重启开发服务器:
#    - Ctrl+C 停止，然后 npm run dev
#
# 4. 在代码中访问:
#    - const apiUrl = import.meta.env.VITE_API_BASE_URL
# ============================================================
